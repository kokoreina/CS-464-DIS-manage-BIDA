<Window x:Class="Dash_boardsBIDA.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login"
        Width="520"
        Height="420"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="#F6F7F9">

    <Grid>
        <Border Background="White"
                Width="380"
                Padding="35"
                CornerRadius="14"
                VerticalAlignment="Center"
                HorizontalAlignment="Center">

            <StackPanel HorizontalAlignment="Stretch">

                <!-- Logo -->
                <TextBlock Text="BIDA MANAGEMENT"
                           FontSize="20"
                           FontWeight="SemiBold"
                           HorizontalAlignment="Center"/>

                <TextBlock Text="Welcome back 👋"
                           Foreground="Gray"
                           Margin="0,5,0,25"
                           HorizontalAlignment="Center"/>

                <!-- EMAIL -->
                <TextBlock Text="Email"
                           FontWeight="SemiBold"
                           Margin="0,0,0,5"/>

                <Border Background="#F3F4F6"
                        CornerRadius="8">
                    <TextBox x:Name="TxtEmail"
                             Height="38"
                             BorderThickness="0"
                             Background="Transparent"
                             Padding="10"/>
                </Border>

                <!-- PASSWORD -->
                <TextBlock Text="Password"
                           FontWeight="SemiBold"
                           Margin="0,18,0,5"/>

                <Border Background="#F3F4F6"
                        CornerRadius="8">
                    <PasswordBox x:Name="TxtPass"
                                 Height="38"
                                 BorderThickness="0"
                                 Background="Transparent"
                                 Padding="10"/>
                </Border>

                <!-- LOGIN BUTTON -->
                <Button Content="Đăng nhập"
        Height="42"
        Margin="0,25,0,10"
        Background="#3E6B5A"
        Foreground="White"
        FontWeight="SemiBold"
        BorderThickness="0"
        HorizontalAlignment="Stretch"
        Click="BtnLogin_Click">

                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="8">
                                            <ContentPresenter
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>

                </Button>

                <!-- EXIT -->
                <Button Content="Thoát"
        Height="35"
        Background="Transparent"
        BorderBrush="#E5E7EB"
        BorderThickness="1"
        HorizontalAlignment="Stretch"
        Click="BtnExit_Click"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>